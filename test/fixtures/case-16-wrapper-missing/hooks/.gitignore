# This fixture's hooks.json.bak is deliberate test data (the pre-patch state
# verify's wrapper_missing repair reads from), not a runtime backup artifact —
# un-ignore it here rather than requiring `git add -f` at commit time.
!hooks.json.bak
