# Everything ships with LF. Git Bash executes the wrapper scripts win-hooks
# generates, and CRLF breaks a shebang line (CASE-02/03).
* text=auto eol=lf

# run-hook.cmd is a cmd.exe/bash polyglot; cmd.exe must not see a BOM or CRLF.
hooks/run-hook.cmd text eol=lf
